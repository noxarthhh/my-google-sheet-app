// *************************************************
// JavaScript สำหรับปรับข้อความ Popup ต้อนรับ
// *************************************************

function getGreetingTime(hours) {
    if (hours < 12) {
        return "สวัสดียามเช้า"; // 00:00 - 11:59
    } else if (hours < 18) {
        return "สวัสดียามบ่าย"; // 12:00 - 17:59
    } else {
        return "สวัสดียามเย็น"; // 18:00 - 23:59
    }
}

function updateWelcomeModal() {
    // 1. กำหนดชื่อผู้ใช้ (User Name)
    // ในระบบจริง: ต้องดึงชื่อผู้ใช้จริงจาก Session หรือ LocalStorage
    // สมมติว่ามีชื่อผู้ใช้อยู่ในตัวแปร 'loggedInUserName'
    const userName = "เจนนิเฟอร์"; // <<< เปลี่ยนเป็นตัวแปรดึงชื่อผู้ใช้จริง
    
    // 2. ตรวจสอบเวลาปัจจุบัน
    const currentHour = new Date().getHours();
    const greeting = getGreetingTime(currentHour);
    
    // 3. สร้างข้อความใหม่
    const newTitle = `${greeting} คุณ${userName} เข้าสู่ระบบ! 🎉`;
    
    // 4. อัปเดต HTML ของ Modal
    const modalTitleElement = document.querySelector('#welcomeModal .modal-title');
    if (modalTitleElement) {
        modalTitleElement.innerHTML = newTitle;
    }
}

// เรียกใช้ฟังก์ชันทันทีเมื่อหน้าเว็บโหลดเสร็จ
document.addEventListener('DOMContentLoaded', updateWelcomeModal);

// *************************************************
// (โค้ดสำหรับ show/close Modal อื่นๆ ตามที่คุณมีอยู่)
// *************************************************